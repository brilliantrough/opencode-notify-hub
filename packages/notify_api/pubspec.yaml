name: notify_api
description: API client and models for the opencode-notify gateway.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  built_collection: '>=5.1.1 <6.0.0'
  built_value: '>=8.9.0 <9.0.0'
  dio: ^5.11.0
  one_of: '>=1.5.0 <2.0.0'
  one_of_serializer: '>=1.5.0 <2.0.0'

dev_dependencies:
  build_runner: ^2.15.1
  built_value_generator: ^8.12.5
  test: ^1.24.0
