{
  "name": "opencode-notify",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:contracts": "pnpm --filter @notify/contracts build",
    "typecheck": "pnpm build:contracts && pnpm -r typecheck",
    "test": "pnpm build:contracts && pnpm -r test",
    "docs:check": "node scripts/check-markdown-links.mjs"
  }
}
